OEE-breakdown split bar — two metrics that sum to 100% across one bar.

```jsx
<MetricBar leftLabel="Running time" leftValue="282h 50m"
  rightLabel="Downtime" rightValue="461h 9m" percent={38}
  fill="var(--cfe-teal)" />
```
