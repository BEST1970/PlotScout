Compact status or count label.

```jsx
<Badge tone="success">Executed</Badge>
<Badge tone="warning">OEE below target</Badge>
<Badge tone="cyan" solid>+2%</Badge>
```

Tones: `neutral | info | success | warning | danger | cyan`. `solid` for filled fills, `size="sm"` for dense tables.
