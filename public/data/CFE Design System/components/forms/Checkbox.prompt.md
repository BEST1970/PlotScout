Selection controls.

```jsx
<Checkbox label="Billable" defaultChecked />
<Switch label="Show app name" defaultChecked />
```

Both accept native input props (`checked`, `onChange`, `disabled`). Navy fill/track signals the selected state.
