Section navigation. Underline tabs get the brand gradient underline on the active item; pill chips suit filter rows.

```jsx
<Tabs items={["Overview", "Trendline", "Breakdown"]} value={tab} onChange={setTab} />
<Tabs variant="pill" items={["Today", "This week", "This month"]} value={range} onChange={setRange} />
```
